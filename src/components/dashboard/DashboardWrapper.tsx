import { DashboardLayout } from "./DashboardLayout";

/**
 * Simplistic wrapper that renders the DashboardLayout.
 * Acts as a bridge between the routing layer and the layout.
 */
export function DashboardWrapper() {
return <DashboardLayout />;

}